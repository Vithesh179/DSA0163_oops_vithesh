#include<iostream>
using namespace std;
int main()
{
	int i,n;
	cout<<"\nenter the size : ";
	cin>>n;
	int *p =new int[n];
	cout<<"enter the values \n";
	for(i=0;i<n;i++)
	{
		cin>>*(p+i);
	}
		cout<<"the values are\n";
		for(i=0;i<n;i++)
		{
			cout<<*(p+i);
		}
		delete []p;
	return 0;
}
